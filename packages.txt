# Extra packages installed beyond Omarchy defaults.
# Regenerate with: ~/scripts/export-packages.sh
#
# Format: one package per line. Lines starting with "#" are comments.
# bootstrap.sh installs these with `omarchy pkg add` (covers Arch `extra`,
# `omarchy` repo) and `omarchy pkg aur add` for the AUR section.

# From the Arch repos
baobab
firefox
openai-codex
openvpn
proton-vpn-cli
proton-vpn-gtk-app
tailscale
wireguard-tools

# From the Omarchy repo
spotify
visual-studio-code-bin
voxtype-bin

# Self-packaged — NOT on AUR. Built from the PKGBUILD at
# https://github.com/pilppilo/codex-omarchy (bootstrap.sh clones it to
# ~/codex-pkgbuild and runs makepkg)
chatgpt
